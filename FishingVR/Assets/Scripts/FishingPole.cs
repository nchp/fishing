using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FishingPole : MonoBehaviour
{

    private float minCastDistance = 5f;
    private float grabDistance = 5f;
    private Vector3 reelCenter;
    private Transform reelControlTransform;
    private Lure fishingLure;
    private FishingRod fishingRod;
    private Reel reelControl;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
